{% for tag in include.page.tags %} {% capture tag_name %}{{ tag }}{% endcapture
%}
<span>
  <a href="/tags/{{ tag_name }}" class="icon">
    <code
      ><div class="icon fa fa-tag fa-xs"></div>
      <nobr>{{ tag_name }}</nobr></code
    ></a
  >
</span>
{% endfor %}
